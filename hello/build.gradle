
apply plugin: 'cpp'
apply plugin: 'google-test'

model {
       
        repositories {
            with getGoogleTestRepository
        }

        components {
            hello(NativeLibrarySpec)
        }

        binaries {
            with getGoogleTestBinary
        }
        
}
